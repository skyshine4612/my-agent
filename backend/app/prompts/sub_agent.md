把复杂任务交给对应业务的专业子 agent 处理，返回其最终结果。
参数：
- subagent_type：业务类型，枚举 ["travel", ...]（各业务见上）
- task：交给子 agent 的完整任务描述，尽量带上所有已知约束（目的地/日期/预算/偏好等）
- name：可选，子会话显示名
何时调用：任务足够复杂、需要该业务的完整规则（如行程规划要同时协调交通/景点/天气/预算）时才调用。简单查询直接调用对应工具，不要 call_sub_agent。
